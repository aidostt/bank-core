# Claude Code Task — Milestone 1: Money moves correctly

Context: read `CLAUDE.md`, `docs/architecture.md`, `docs/adr/*`, and
`docs/services/{ledger,transfer,identity,account,api-gateway}-service.md` (gateway
file is `api-gateway.md`) before writing code. They are binding.

Build, in order:
1. Repo scaffold per CLAUDE.md layout: go.work, pkg/{money,config,logging,apperr,
   grpcx,outbox,pgtx}, proto module + buf config + `make generate`, golangci-lint
   config (enable: govet, errcheck, staticcheck, depguard [domain packages: stdlib
   only], gosec, sqlclosecheck), Makefile targets from CLAUDE.md, compose `core`
   profile (postgres+init-db.sql, redpanda, redis, toxiproxy, 7 service builds),
   multi-stage distroless Dockerfiles, .env.example.
2. identity-service per its doc (skip outbox event until M2 if it saves time — flag
   it in the doc if so).
3. account-service: OpenAccount (sync ledger CreateAccount), ResolveByNumber,
   Freeze/Unfreeze, GetAccount/List. Projections/consumers are M2.
4. ledger-service **fully** per its doc. Do not simplify invariants, locking order,
   partitioning, idempotency, or the outbox. Include the concurrent conservation
   test and `make verify-ledger`.
5. transfer-service **fully** per its doc: state machine (pure + exhaustively
   tested), hold/capture saga, idempotency keys, FX with seeded rates, limits,
   recovery worker, outbox.
6. gateway per its doc: all `/v1` routes for the M1 scenario, JWKS auth, RBAC
   table, rate limit, Idempotency-Key enforcement, problem+json mapping.
7. `make demo` script (bash+curl+jq): two users, KZT+USD accounts, top-ups,
   INTERNAL FX transfer, P2P transfer, prints balances before/after, ends with
   verify-ledger.

Definition of done — verify each, do not self-report success without running:
- [ ] `make up && make demo` from clean checkout succeeds
- [ ] `make test` green incl. integration (testcontainers)
- [ ] concurrent conservation test green (100 goroutines)
- [ ] recovery test: kill ledger between hold and post (integration harness or
      toxiproxy), transfer resolves; verify-ledger clean
- [ ] idempotency: same key 20 parallel CreateTransfer → 1 transfer
- [ ] `make lint` clean; no float money anywhere; domain packages import no IO
- [ ] service docs updated if any deviation was necessary (deviations require an
      ADR update in the same commit)

Commit style: atomic commits per component, conventional commits
(`feat(ledger): ...`), each commit compiles and passes lint.
