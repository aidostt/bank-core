# Milestone 2: Distributed behaviors + observability

Prereq: M1 accepted. Re-read `docs/architecture.md` §5, §7, §8 and the service docs
for account, antifraud, notification.

Build:
1. `pkg/kafka` consumer runtime: group consumer, per-message handler contract,
   dedup helper (processed_messages in caller's tx), exponential backoff retries
   (1→16s, 5 attempts), DLQ producer with error headers, otel propagation from
   Kafka headers, lag metrics. One implementation, used by all three consumers.
2. account-service consumers: ledger.transactions → version-guarded balance
   projection; fraud.alerts(HIGH) → freeze + AccountFrozen event. Wire projected
   balances (+as_of) into GET /v1/accounts.
3. identity `customers.registered` event + account-service customer bootstrap
   consumer (if skipped in M1).
4. anti-fraud-service per doc: stats, 4 rules, alerts via outbox.
5. notification-service per doc.
6. Observability: pkg/otel finalized; compose `obs` profile (prometheus, grafana
   provisioned with the 2 dashboards as code, jaeger); RED + business metrics per
   architecture §8; every log line carries request.id.
7. tests/e2e: full scenario incl. fraud freeze (burst of transfers → HIGH alert →
   account FROZEN → next transfer rejected ACCOUNT_FROZEN) and notification row
   assertions.
8. `make dlq-inspect`, replay demo target (`make replay-projections`).

DoD:
- [ ] e2e green; runnable in CI job
- [ ] duplicate delivery test: same event twice → exactly one side effect (each consumer)
- [ ] out-of-order projection test green
- [ ] poison message reaches DLQ with attempts/error headers
- [ ] Jaeger shows one trace across gateway→transfer→ledger→(kafka)→account consumer
- [ ] dashboards render with data after `make demo`; screenshots saved to docs/img/
- [ ] `make replay-projections` converges to verify-ledger truth
